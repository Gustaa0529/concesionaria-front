export class Imagen {
    nombre: string;
    ruta: string; 
    idVehiculo: number;
  
    constructor(nombre: string, ruta: string, idVehiculo: number) {
      this.nombre = nombre;
      this.ruta = ruta;
      this.idVehiculo = idVehiculo;
    }
  }
  